import { CurrencyUsdFormatDirective } from './currency-usd-format.directive';

describe('CurrencyUsdFormatDirective', () => {
  it('should create an instance', () => {
    const directive = new CurrencyUsdFormatDirective();
    expect(directive).toBeTruthy();
  });
});
